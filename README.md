# 🔒 Sentinel Agent v2.0

**Zero-Trust AI Agent with Game Automation & Computer Vision**

> Локальный агент с архитектурой, вдохновленной EDR системами. Полный контроль, нулевое доверие, абсолютная безопасность.

---

## 🎯 Основные возможности

### 🧠 Интеллектуальное планирование
- Интеграция с Google Gemini API
- Стратегическое планирование задач
- Адаптивное выполнение с обучением
- Анализ ошибок и переплан

### 🎮 Игровая автоматизация

### 🔒 Zero-Trust архитектура
- ✅ 8 слоёв безопасности
- ✅ Изолированный executor (Worker Threads)
- ✅ Path jail + sandbox
- ✅ SHA256 integrity checks
- ✅ Approval gates для всех действий
- ✅ Immutable audit logs
- ✅ Kill switch
- ✅ Strict command whitelist

---

## 📦 Требования

```bash
Node.js 18+
npm 8+
Позволение на управление мышью/клавиатурой (OS level)
```

---

## 🚀 Установка

```bash
# Клонируем репо
git clone https://github.com/Fisterovna2/sentinel-agent.git
cd sentinel-agent

# Устанавливаем зависимости
npm install

# Настраиваем API ключ
cp .env.example .env
# Редактируем .env и добавляем свой Gemini API key

# Запускаем агент
npm start
```

---

## 🔑 API Ключ (Gemini)

1. Получи ключ на https://aistudio.google.com/app/apikeys
2. Скопируй в `.env`:
   ```
   GEMINI_API_KEY=your_key_here
   ```

## 📡 Архитектура

```
React UI (Frontend)
    ↓ HTTP/WebSocket
Node.js Main Server (Мозг)
    ├── Gemini Proxy
    ├── Policy Engine
    ├── Approval Manager
    ├── Integrity Checker
    └── Audit Logger
    ↓ IPC (Worker Messages)
Executor Worker (Изолированный исполнитель)
    ├── robotjs (mouse/keyboard)
    ├── OpenCV (computer vision)
    └── Sandbox filesystem
```

---

## 🛡️ Безопасность

### Агент НЕ МОЖЕТ (физически):
- ❌ Изменить свой код
- ❌ Выйти за пределы sandbox
- ❌ Писать в мессенджеры без разрешения
- ❌ Скачать и запустить вирусы
- ❌ Скрыть логи
- ❌ Обойти approval gates
- ❌ Получить права без твоего клика

### Почему это работает:
1. **Capability-based security**: права не у модели, а у архитектуры
2. **Immutable core**: код агента readonly при запуске
3. **Worker isolation**: executor живёт в отдельном процессе
4. **Audit trail**: каждое действие логируется
5. **Human in the loop**: критичные действия требуют твоего approval

---

## 🎮 Примеры использования

### Dota 2 - Автофарм
```bash
POST /api/execute
{
  "step": {
    "desc": "Farm jungle neutral camp for 5 minutes",
    "risk": 4,
    "command": "dota.farm(x=1000, y=1000, duration=300)"
  }
}
```

### Roblox - Автоматизация игры
```bash
POST /api/execute
{
  "step": {
    "desc": "Collect eggs in Pet Simulator X",
    "risk": 2,
    "command": "roblox.collect_items(type=\"egg\", count=100)"
  }
}
```

---

## 📊 Мониторинг

Логи хранятся в `sandbox/`:
- `audit.log` — все действия
- `security.log` — попытки нарушений

```bash
tail -f sandbox/audit.log
```

---

## 🔧 Конфигурация

`agent-server-v2.js`:
```javascript
const PERMISSIONS = {
  filesystem: false,    // По умолчанию OFF
  mouse: false,         // По умолчанию OFF
  keyboard: false,      // По умолчанию OFF
  network: false,       // По умолчанию OFF
  screenshot: true      // Только чтение
};
```

Если нужна мышь — включишь через UI.

---

## 🎯 Дорожная карта

- [ ] React UI интерфейс
- [ ] Поддержка больше игр
- [ ] OpenCV интеграция для vision
- [ ] Обучение на replay файлах
- [ ] Web версия агента
- [ ] Multi-agent синхронизация

---

## 📝 Лицензия

MIT License — используй как хочешь, но сохраняй attribution.

---

## 👨‍💻 Автор

**Fisterovna2** — студент-разработчик, специалист по AI/ML и game automation.

GitHub: [@Fisterovna2](https://github.com/Fisterovna2)

---

## ⚠️ Дисклеймер

**Используй Sentinel только на своём компьютере!**

- Не используй для игр, где это нарушает ToS
- Не используй для вредоносных целей
- Не распространяй скомпилированные версии
- Полная ответственность на тебе

Этот проект — образовательный и демонстрирует архитектуру безопасных AI агентов.
