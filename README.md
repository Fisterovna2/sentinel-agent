[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Node.js](https://img.shields.io/badge/Node.js-18%2B-green.svg)](https://nodejs.org)
[![Status](https://img.shields.io/badge/Status-Active-brightgreen.svg)](#)
[![Version](https://img.shields.io/badge/Version-2.0.0-blue.svg)](#)

# 🔒 Sentinel Agent v2.0

Локальный Zero‑Trust AI‑агент для автоматизации игр и задач с компьютерным зрением — безопасно, прозрачно и под полным контролем пользователя.

> Простыми словами: это "умный бот", который планирует и выполняет шаги, но не делает ничего без контроля и разрешений. Архитектура вдохновлена EDR‑подходами: изоляция, проверка целостности и журналирование — всё для безопасности и предсказуемости.

---

## 🎯 Что умеет (кратко и по-человечески)

- 🧠 Интеллектуальное планирование
  - Использует Google Gemini через proxy для генерации планов.
  - Делит задачу на шаги, оценивает риски и корректирует поведение при ошибках.
- 🎮 Игровая автоматизация
  - Управление мышью/клавиатурой, шаги для сборов/квестов и т.п.
- 🖼️ Компьютерное зрение (план)
  - Интеграция с OpenCV для поиска объектов на экране.
- 🔒 Zero‑Trust безопасность
  - Многоуровневая защита: изоляция исполнителя, whitelist команд, immutable логи и т.д.

---

## Почему стоит использовать именно так

- Полный контроль у пользователя — модель никогда не получает лишних прав.
- Все действия логируются и доступны для аудита.
- Критичные операции требуют явного подтверждения (human-in-the-loop).
- Агент проектировался так, чтобы минимизировать риск вреда системе.

---

## 📦 Требования

- Node.js 18+
- npm 8+
- Права ОС на управление мышью/клавиатурой (если планируете использовать automation)
- Доступ к Gemini API (опционально, для улучшенного планирования)

---

## 🚀 Быстрый старт

1. Клонируем репозиторий:
```bash
git clone https://github.com/Fisterovna2/sentinel-agent.git
cd sentinel-agent
```

2. Устанавливаем зависимости:
```bash
npm install
```

3. Настраиваем переменные окружения:
```bash
cp .env.example .env
# Откройте .env и добавьте ваш GEMINI_API_KEY (если используете Gemini)
```

4. Запуск:
```bash
npm start
```

После старта откроется сервер (или UI, если он реализован) — все действия будут происходить локально.

---

## 🔑 Настройка API ключа (Gemini)

1. Получите ключ: https://aistudio.google.com/app/apikeys  
2. В .env добавьте:
```
GEMINI_API_KEY=your_key_here
```
(Если ключа нет — агент всё равно может работать в базовом режиме без продвинутого планирования.)

---

## 🏗️ Архитектура (упрощённо)

```
React UI (Frontend)
    ↓ HTTP / WebSocket
Node.js Main Server (Мозг)
    ├── Gemini Proxy          # опционально
    ├── Policy Engine         # решение о разрешениях
    ├── Approval Manager      # UI -> подтверждения
    ├── Integrity Checker     # SHA256 проверка
    └── Audit Logger          # immutable логи
    ↓ IPC (Worker Messages)
Executor Worker (Изолированный процесс)
    ├── robotjs (mouse/keyboard)
    ├── OpenCV (vision)      # опционально
    └── Sandbox filesystem
```

Ключевая идея: основной процесс управляет политиками и логированием, исполняющий процесс работает в изоляции и с минимальными правами.

---

## 🛡️ Безопасность — что важно знать

Агент спроектирован так, чтобы ограничить возможную "самонадеянность" модели.

Агент НЕ МОЖЕТ (физические и архитектурные ограничения):
- ❌ Изменить свой код во время выполнения
- ❌ Выйти за пределы sandbox
- ❌ Писать в мессенджеры без явного разрешения
- ❌ Скачать и запустить произвольные бинарники без approval
- ❌ Скрыть или удалить логи
- ❌ Обойти approval gates
- ❌ Получить права администратора без вашего вмешательства

Почему это работает:
1. Capability‑based security — права выдаются архитектурой, а не модели.  
2. Immutable core — важные файлы только для чтения при запуске.  
3. Worker isolation — исполняющий код в отдельном процессе.  
4. Audit trail — каждое действие фиксируется в логе.  
5. Human‑in‑the‑loop — критичные команды требуют подтверждения.

---

## ⚙️ Конфигурация (пример)

Файл: `agent-server-v2.js`
```javascript
const PERMISSIONS = {
  filesystem: false,    // По умолчанию OFF
  mouse: false,         // По умолчанию OFF
  keyboard: false,      // По умолчанию OFF
  network: false,       // По умолчанию OFF
  screenshot: true      // Только чтение
};
```
Если хотите разрешить управление мышью/клавиатурой — включите соответствующие флаги через UI / конфиг.

---

## 📡 Пример API — запуск шага (Roblox)

POST /api/execute
```json
{
  "step": {
    "desc": "Collect eggs in Pet Simulator X",
    "risk": 2,
    "command": "roblox.collect_items(type=\"egg\", count=100)"
  }
}
```

Важно: все команды проходят через Policy Engine и, при необходимости, требуют approval пользователя.

---

## 📊 Мониторинг и логи

Логи хранятся в sandbox/:
- `audit.log` — все действия агента (immutable)
- `security.log` — попытки нарушений и события безопасности

Посмотреть в реальном времени:
```bash
tail -f sandbox/audit.log
```

---

## 🧭 Дорожная карта (что планируется)

- [x] Полнофункциональный React UI (превью, аналитика, управление правами)
- [x] Поддержка большего числа игр / платформ
- [x] OpenCV интеграция для распознавания и трекинга
- [x] Обучение на replay-логах для улучшения планирования
- [x] Web-версия агента (ограниченная)
- [x] Координация нескольких агентов (multi-agent sync)

---

## 📝 Лицензия

MIT License — свободно использовать и модифицировать при сохранении attribution.

---

## 👨‍💻 Автор

**Fisterovna2**
Discord: [@fistrovna](https://github.com/Fisterovna2)

Если найдёте баги или хотите предложить улучшение — откройте issue или PR.

---

## ⚠️ Дисклеймер (прочитайте внимательно)

Используйте Sentinel только на своём компьютере и в легальных целях:
- Не используйте в играх или сервисах, где это запрещено (ToS).
- Не используйте для вредоносных действий.
- Не распространяйте скомпилированные версии без разрешения.
- Полная ответственность за использование лежит на вас.

Проект носит образовательный характер и демонстрирует подходы к безопасной автоматизации.

---
